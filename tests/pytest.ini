[pytest]
markers =
    storage: Storage/task management tests
    risk: Risk calculation tests
    jira: JIRA integration tests
    search: Semantic search tests
    lifecycle: Cross-script lifecycle tests
testpaths = tests
